#!/usr/bin/env ruby
# encoding: utf-8

require 'thor'

class Mcp < Thor
  desc 'check [MAIL-ADDRESS]','check [MAIL-ADDRESS] check the availability of the mail address'
  def check(mail)
    
  end
end

Mcp.start
